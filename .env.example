# Copy this file to `.env.local` and fill in your own values.
# These are SAFE to expose to the browser — Supabase's anon/publishable
# key is designed for client-side use. NEVER put the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
